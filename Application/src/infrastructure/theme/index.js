import { colors } from './colors';
import { fonts, fontSizes, fontWeights } from './fonts';
import { spaces } from './spacing';

export const theme = {
  colors,
  fonts,
  fontSizes,
  fontWeights,
  spaces,
};
