import React from 'react';

const MainTitle = ({ title }) => {
  return (
    <h1 className="text-8xl font-semibold my-12 text-center uppercase">
      {title}
    </h1>
  );
};

export default MainTitle;
